# MongoDB connection string. Create a free cluster at https://www.mongodb.com/atlas
# or run MongoDB locally, then set this value.
MONGODB_URI=mongodb://127.0.0.1:27017/cnvt

# Optional: name of the database if not included in the URI above
MONGODB_DB=cnvt
